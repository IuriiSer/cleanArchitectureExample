const Profile = require('./Profile');
const Event = require('./Event');
const Common = require('./Common');
const Authorization = require('./Authorization');

module.exports = {
  Profile, Event, Common, Authorization,
};
